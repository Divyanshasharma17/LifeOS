# Dashboard is primarily a read-only aggregation layer over other apps'
# models (Goals, Tasks, and later Study/Focus/Habits/Health/Mood/Journal).
# It intentionally defines no models of its own in this release; its views
# query other apps directly through the ORM.
